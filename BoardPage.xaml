<Page
    x:Class="ygc.BoardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ygc"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
            <RowDefinition Height="100" />
        </Grid.RowDefinitions>
        
        <TextBox Grid.Row="2" x:Name="TurnTextBox" Text="1" Margin="0,10,203,0"></TextBox>
        <TextBox Grid.Row="2" x:Name="ResponsiveTextBox" Text="0" Margin="203,10,0,0"></TextBox>
        <TextBox Grid.Row="2" x:Name="TapXTextBox" Margin="0,52,203,0"></TextBox>
        <TextBox Grid.Row="2" x:Name="TapYTextBox" Margin="203,52,0,0"></TextBox>
    </Grid>
</Page>
